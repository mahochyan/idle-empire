# IE-007 · 资源类型与建筑系统 · 交付报告

- 状态：完成待审查（本轮实现全部自检通过；W1-W4 起点值与视觉目检等用户确认项见 §6）
- 基线：master @ `98571e38801f71bfdbb982a626e0e58f510abb98`（IE-001 分支 `ie001-save-safety` 之外的独立工作区改动，**未提交未推送**）
- 依据：`docs/codex/IE-007-resource-building-system.md` 方案（经用户确认方向）、`策划文档.md` v1.1

## 1. 修改了什么（逐文件）

| 文件 | 改动 |
|---|---|
| `config.js` | CFG.res 新增 copper/iron/coin（type/max/maxPerLv/desc）；CFG.buildings 新增 mine/smelter/mint/market 四座（needBoss/consumes/produces/upCostLv）并为现有 10 座补 `type` 只读标注；buildingCaps 增 production/utility 两档；新增 CFG.market.rates 4 条汇率 |
| `math.js` | SAVE_VERSION 1→2；S 初值/`_legacyDefaults` res 补 3 新键；validateSave 增 v2 新键必需规则（v1 宽容）；migrateSave 增 v1→v2 补键（幂等）；新增 `producerKey/resCap/passiveProduction/exchangeResource`；tick 挂接被动产出 |
| `ui.js` | updateUI 渲染新资源 3 格＋**资源增减视觉反馈**（`_prevResUI` + `flashRes`：CSS-only 浮泡 +N/-N 与数值闪色绿/红）；BUILD_CATEGORIES 新增 economy 页签；rBuildCard 被动产出/消耗标识；marketPanel/marketExchange 市场兑换面板 |
| `index.html` | topbar 新增 res-copper/iron/coin 三格；`.res-delta` 浮泡 CSS 与 `@keyframes resFloatUp`、`.val.up/down` 闪色、prefers-reduced-motion 降级 |
| `sprites.js` | PIX_SPRITES 新增 copper/iron/coin 三枚像素图标 |
| `tests/ie001/run.js` | fixture（fullLegacy/zerosLegacy）补新键；S01/S02/S09 版本断言 v1→v2；新增 V01-V06 六用例 |
| `tests/ie001/browser_smoke.js` | v 断言更新；新增 IE007 四项（增减气泡/闪类/topbar 新资源/经济页四建筑/市场面板）＋3 张截图 |
| `tests/ie001/browser_interact.js` | v 断言更新（v===2） |

## 2. 新增配置与关键数值（调参表 · 全部在 config.js，无硬编码）

| 项 | 位置 | 值（保守起点·待推演） |
|---|---|---|
| 铜 | `CFG.res.copper` | type passive；max 2000；maxPerLv 500；矿井 +2/s/级 |
| 铁 | `CFG.res.iron` | type passive；max 1500；maxPerLv 400；冶炼 +1/s/级（耗铜 3/s/级） |
| 金币 | `CFG.res.coin` | type currency；max 5000；maxPerLv 1000；铸币 +2/s/级（耗粮 5/s/级） |
| 矿井 | `CFG.buildings.mine` | needBoss 5；upCostLv 1.15；产出 copper |
| 冶炼厂 | `CFG.buildings.smelter` | needBoss 10；consumes copper 3 / produces iron 1 |
| 铸币厂 | `CFG.buildings.mint` | needBoss 15；consumes food 5 / produces coin 2 |
| 市场 | `CFG.buildings.market` | needBoss 10；兑换面板挂接 |
| 汇率 | `CFG.market.rates` | coin→wood 8、coin→stone 6、wood→coin 0.1、stone→coin 0.14（往返乘积<1 无套利，V05 断言） |
| 停产规则 | `passiveProduction`（math.js） | 原料不足当次停产，不扣负不产出（V04 断言） |

## 3. 存档兼容（v2）

- `SAVE_VERSION=2`；`S.res` 新增 3 键；v1→v2 迁移补 0、幂等、PRE 原文保留（V02）；坏新键（如 res.copper='x'）进保护且原文不被覆盖（V03）；未知资源键仍拒绝（V06 回归 S12 语义）；IE-006 的 `S.daily` 后续仍并入 v2（未发布可扩展）。

## 4. 验证结果（全部实际运行）

```
node --check config.js / math.js / ui.js / sprites.js        exit 0
node tests/ie001/run.js               # node v24.19.0      29/29 通过，exit 0
node tests/ie001/browser_smoke.js     # Edge headless=new  33/33 通过，exit 0
node tests/ie001/browser_interact.js                        28/28 通过，exit 0
```
- 新增用例：V01 v2 往返 / V02 迁移幂等 / V03 坏键保护 / V04 被动产出·上限·停产 / V05 兑换·套利 / V06 未知键拒绝。
- 回归：既有 S01-S12b、S08 系列、S09 系列、S11 系列、R01/R02 全部保持通过；浏览器 S13/S14 全链路零未捕获异常。
- 截图（assets/）：`IE007-01-delta-flash-360.png`（增减浮泡时刻）、`IE007-02-build-economy-360.png`（经济建筑页）、`IE007-03-market-panel-360.png`（市场兑换面板）。

## 5. 视觉复查说明（诚实声明）

- 本执行会话为**文本模型**，`read_image` 返回"模型仅接受文本"，**我无法直接目检截图**；因此视觉复查采用：①DOM/样式级断言（气泡文本、up/down 类挂载、topbar 渲染、360 宽度下 `scrollWidth<=innerWidth` 不溢出——interact T1 在本改动后仍通过）；②三张实时截图产出。
- **"看图"这一环交给你或视觉模型**：请复查 `IE007-01/02/03` 三张图（浮泡是否可读、经济页布局、市场面板在 360 宽下是否不挤）。
- 资源增减直观性实现=CSS-only：`+N/-N` 浮泡（1s 周期跟随 tick 脉冲）+数值绿/红闪色；`prefers-reduced-motion` 下动画关闭、颜色仍可区分。

## 6. 待确认项

- W1 数值起点（§2 调参表）——推演属 IE-006 阶段，本批保守落位。
- W2 市场每日次数/多汇率——IE-006 补齐（本批仅基础汇率+面板）。
- W3 停产规则"当次停产"——已按建议实现，可改。
- W4 解锁门槛 needBoss 5/10/15/10——已按示例落位。
- 学院（科技来源 P0-1 断头路）——按决策留给 IE-002，本批未动。
- 视觉目检结论——等你/视觉模型回执。

## 7. 回退与风险

- 回退：`git checkout -- config.js math.js ui.js index.html sprites.js` 还原 5 个生产文件；测试改动随 tests/ 一并还原即可。存档影响：v2 档被回退后的旧代码读取时，3 个新 res 键被旧 serialize 丢弃（进度字段其余无损）——与 IE-001 报告 §9 同款语义。
- 风险：①被动产出与军粮/口粮叠加的食物消耗（V04 展示联动）需 IE-006 推演定参；②市场汇率未来扩展多汇率时须保持"往返乘积<1"不变式（V05 断言守护）；③浮泡 1s 动画在低端 WebView 为纯 transform/opacity，无绘制热点。
- 未提交未推送；不删任何现有资源/建筑；本批未触碰科技/伤害/Boss/兵种数值。

## 8. 与 IE-006 衔接

- 本批=资源类型与建筑体系落地（v2 一次完成）；IE-006=曲线机制（分段成本/免维护带/转换阀每日上限/离线）在其上叠加，`S.daily` 仍并入 v2；推演脚本以本批 config 为输入。
