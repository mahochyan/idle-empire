# 已修改进度总结（2026-09-22 · 单一进度事实源）

- 本文件整合原《改造点清单》《分期计划》与 16 份切片报告、《总验收报告》——原文件全部归档到 `docs/_archive_20260922_integration/`
- **配套两份文档**：`docs/策划总纲.md`（策划事实源）+ 本文件（进度事实源）
- 前提：本项目无服务端（纯静态），改造＝**本地等价实现**；**未提交、未推送**，改动全部在工作区
- 当前状态：**批准计划内可实施切片全部完成并逐片验证**；剩余 2 片 + 2 项数值对齐待裁决（§5）

---

## 一、最终验收（全绿）
| 项 | 命令 | 结果 |
|---|---|---|
| L1 逻辑回归 | `node tests/ie001/run.js` | **96 / 96** |
| L2 真实浏览器（冒烟） | `node tests/ie001/browser_smoke.js` | **41 / 41** |
| L2 真实浏览器（交互/窄屏） | `node tests/ie001/browser_interact.js` | **28 / 28** |
| 零行为对拍 | `node tools/verify/equiv.js` | 全开关关闭 ⇒ 与 HEAD **逐字节相同** |
| 可达性 | `node tools/verify/reachability.js` | 资源科技 **6/6** 纯挂机可达 |
| 离线结算探针 | `node tools/verify/probe-offline.js` | 木 +4320 / 石 +2160 / 食 +3996（Δ3600s×0.6） |
| 机器状态 | — | Edge 24 进程 / headless 0 / ≈2.7 GB |

**验收标准逐条**：五份产出 ✅｜端到端（启动→离线结算→操作→落库）✅（L2 六条 + 截图）｜时间篡改有界 ✅｜幂等防重发奖 ✅｜回归全绿 ✅｜**回滚演练实际执行** ✅

---

## 二、改动文件（9 个，均为开关/新增，原始数值保留）
| 文件 | 主要改动 |
|---|---|
| `config.js` | `offline`/`idem`/`caps(+expand+ownMax)`/`upkeep`/`tech(sciencesNoMerit/longLadder)`/`food`/`pop`/`unitCapBoost`/`save.v3`/`market(12 汇率+日限)`/`townGate(地契九档)`/`res.deed`/`sciencesLong`/`diag` |
| `math.js` | 诊断日志、幂等原语、离线结算与推进、存档 v3、上限/免维护带/人口/日界/地契门；`tick()` 计时块**等价抽出** |
| `ui.js` | 离线报告卡、回前台结算、村民/地契需求/市场剩余次数展示（展示同源） |
| `technology.js` | 科技表走生效表；兵种研究幂等 |
| `sprites.js` | 新增 `deed` 像素图标 |
| `index.html` | `APP_VERSION/APP_SCRIPTS` 自检锚点（`?v=` 纪律不变） |
| `tests/ie001/*` | `run.js` 96 例、`browser_smoke.js` 41 例、`browser_interact.js` 28 例、`curve-sim.js` 推演（含沙箱旋钮） |
| `tests/ie001/edge-reaper.js` | **浏览器实例清理机制**（预扫+退出钩子+同步全树击杀+并发锁+profile 回收） |
| `tools/verify/*` | `equiv` / `reachability` / `probe-offline` / `config-boundary-audit` / `era_*`（解包实证，只读） |

---

## 三、已实施切片（19 项 · 每项都有 L1 逐值断言 + 对拍守护）
| # | 内容 | 开关（默认） | 关键证据 / 效果 |
|---|---|---|---|
| 1 | curve-sim 推演脚本 | — | 34 锚点复验；确定性复跑零差异 |
| 2 | 诊断环形日志 + 开关骨架 | `diag`(on) | W01-W04；**首次建立"零行为对拍"** |
| 3 | 资源科技去战功（**解死锁**） | `tech.sciencesNoMerit`(true) | 6/6 可达；铁/金币 0→1900/6000 |
| 4 | 上限空间扩容 | `caps.expanded`(true) | 仓库 20000→60000、科技 700→5000 |
| 4b | 食物经济对齐（R5-①②） | `food.aligned`(true) | **升级启动 0→42**；食物 −2.74→+2.79/s |
| 5 | 科技长阶梯（3→6 节点） | `tech.longLadder`(true) | 节点 30s/90s/250s/610s/1110s/2530s |
| 6 | 军粮免维护带 + 分段斜率 | `upkeep.freeBand`(true) | 六点边界逐值；带兵余粮 +0.28/s |
| 7 | 人口结构（派生式增长） | `pop.growth`(true) | 基础 4 起步；**不新增存档字段** |
| 8 | 曲线定参复跑 | — | 三阶段拟合判定 + 根因 G1-G6 |
| 8a | 单位上限上调（O8） | `unitCapBoost`(true) | 步兵营 lv5：35 → 80 |
| 8b | 建筑上限改自身 LvMax（R2=A 半） | `ownMax`(true) | **`需升级城镇` 从拒因 top5 消失** |
| 9 | **存档 v3**（ops/offline/daily） | `save.v3`(true) | 迁移矩阵 + 坏字段保护 + v4 拒收 |
| 10 | **幂等层**（窗口 5s、持久化） | `idem`(true) | 只扣一次 / 窗口外可重 / 环形 ≤200 |
| 11 | **离线结算 + 报告卡** | `offline.enabled`(true) | L2 端到端 + 截图；**修复木/石净速率为 0 真 bug** |
| 12 | **离线推进** + 驻军冻结 | `offline.advance`(true) | 闭式 vs 逐秒对拍；**修复 off-by-one** |
| 13 | **交易所多汇率 + 日限 + 价差** | `market.multiRate`(true) | 12 汇率无套利；日限 5 次；跨日重置 |
| **S8c** | **加入地契 + 城镇门改「地契+科技」** | `townGate.useDeed`(true) | 村民 10→**40**；升级 49→**100**；食物 +3.79→**+37.65/s** |
| 16 | 收尾（版本纪律/边界表/文档同步） | — | P01 自检 + L2 版本锚点 |

**关键量化对比（curve-sim 10h）**：切片 1 时"0.17h 全面触顶、升级 0 次" → 现在"**50h 持续升级 98 次、城镇 Lv4、村民 40**"。

---

## 四、本轮抓出并修复的真缺陷（全部已回归）
1. **木/石离线净速率为 0**：`type==='basic'` 判据错误（木/石无 type 字段）→ 探针复现 → 改判据
2. **离线推进 off-by-one**：完成通道对非到期建筑多扣 1 秒 → 非到期项扣 `secs−1` 补偿 + 闭式 vs 逐秒对拍锁死
3. **学院扩容分支未短路**：lv19 被 barracks 回退误锁
4. **分析工具口径缺陷 2 处**：食物耗粮用原始值而非生效值；H01 期望值"自证"（已改独立口径）
5. **浏览器实例泄漏**：一度 280 进程 / 24.7 GB → 建成 `edge-reaper` 并实测（正常 0 残留；强杀后 CLI 清 2 个 / 267 MB）

---

## 五、未实施项（**等你裁决**；候选与影响都已算好）
| # | 项 | 候选 / 影响 |
|---|---|---|
| 1 | **切片 14** 科技收入改占人口学者制（C1/C2） | 需定形态（新增学者轨道 or 改造学院使其占人口）；城镇门已开，现在可做 |
| 2 | **切片 15** 被动建筑人口约束 + 停产标识（C5/C4） | 需定占人口数与停产规则 |
| 3 | **S2 科技阶梯重定时** | 候选 **×80 + 科技上限×20** → 节点 0.29h/0.68h/1.33h/2.29h/4.16h/**16.2h**（补 3-15h 中期墙） |
| 4 | **S9 成本剖面换竞品分段线性** | 50h 实测：采集建筑 3/3/2 → **9/7/9**；消后期食物枯竭；升级 98→117 |
| 5 | 地契细节：命名 / 100 金币→1 地契 / 成本曲线（地契 5…520 + 科技 0…12000）/ 是否加"Boss 减免" | 现用「地契」；细节一句话即可调 |

---

## 六、回滚路径（已实际演练一次）
| 层级 | 操作 |
|---|---|
| 关开关（推荐，保留数据） | `CFG.<切片>.enabled=false`（如 `offline.enabled`、`idem.enabled`、`townGate.useDeed`、`save.v3`…） |
| 代码回滚 | `git checkout -- <9 个文件>`（演练记录：备份 → 回 HEAD → 回滚态 L1 32/32 + L2 33/33 全绿 → 恢复 → L1 96/96 且对拍一致） |
| **注意** | 回滚到 HEAD 会**丢失 v3 字段语义**（`ops/offline/daily` 被丢弃，进度无损）——**不宣称完全兼容** |

---

## 七、已知风险与诚实声明
- 所有数值（地契曲线/长阶梯成本/免维护带段宽/食物对齐）均为**候选值**，仅用户明确裁决过的已启用
- **L3 真机未运行**（无 Android 设备）、**L4 线上未部署**——一律标注"未运行"，不以任何形式冒充通过
- 未提交、未推送；如要上线需按纪律把 `index.html` 的 `V` +1 后再部署
