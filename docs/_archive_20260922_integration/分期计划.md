# 分期计划 v2（阶段 3 产出 · 已按"骨架拟合"指令重排 · 待你确认后逐片执行）

- 设计指令（2026-09-22）：**骨架拟合《放置时代》曲线，外皮保留我方特色**；特色待骨架完成后再发展
- 原则：可独立验证的切片排序；每片遵守 §0 固定循环；每片结束**停下等你确认**
- 验证一律按 `实机验证标准.md`（L1/L2/L3/L4 + 阈值 + 证据归档）

## §0 每片固定循环（不得跳步）
1. 先写测试/对拍脚本，跑一遍确认**当前行为基线**（贴命令+输出+退出码）
2. 改代码，范围**只限本片**
3. 跑测试（贴命令+完整输出+退出码）
4. **实机验证**（本片归属层；L1+L2 必跑基线回归），归档证据（日志/截图/存档 diff）
5. 更新 `改造点清单.md` 状态
6. **停下汇报**，等确认再进下一片

## 切片总表（v2 重排）

### M0 推演基线
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证 | 回滚 |
|---|---|---|---|---|---|---|
| 1 | **curve-sim 推演脚本** ✅**已完成** | A1 | 新增 `tests/ie001/curve-sim.js` | 低 | L1：exit 0 / 确定性 / 四轨输出；L2：基线回归 | 删除文件 |

### M1 基建（零行为变化，为后续所有片提供开关与诊断）
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证 | 回滚 |
|---|---|---|---|---|---|---|
| 2 | 开关骨架 + 诊断环形日志 | A2 A3 | `config.js`（`CFG.offline/idem/passive/tech.enabled=false`）、`math.js`（`logDiag` 50 条不入档） | 低 | L1+L2：基线回归全绿；开关关闭时**存档 diff 为空** | 开关保持 false / revert |

### M2 骨架拟合（**用户优先项**；只调数值与结构形态，不动命名/题材=皮）
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证（阈值） | 回滚 |
|---|---|---|---|---|---|---|
| 3 | **解死锁**：资源科技去战功依赖 + 可达性检查 | S1 | `config.js@322-324`、`math.js@559`、`tests` | 中 | L1：纯挂机下 `sci_iron/sci_coin` 可达（战功=0 也能研）；可达性脚本对全部解锁项输出"有门" | `CFG.sciences.needMerit` 回滚（保留原值备份） |
| 4 | **上限空间扩容** ✅**已完成** | S3 | `config.js caps`、`math.js@72/77/534` | 中 | L1：Y01-Y03 + equiv；curve-sim：上限 60000/5000/7600/24000 | `CFG.caps.expanded=false` |
| **4b** | **R5 食物经济对齐**（新增·用户裁决 B） ✅**已完成** | S7 | `config.js food`、`math.js@42/559`、`ui.js@286` | 中 | L1：Z01-Z03 + equiv；curve-sim：**升级 0→42**、学院 lv20、食物 +2.79/s 未转负 | `CFG.food.aligned=false` |
| 5 | **成本阶梯扩节点** ✅**已完成** | S2 | `config.js sciencesLong`、`math.js activeSciences`、`technology.js@243`、`ui.js@290` | 中 | L1：A01-A03 + X03（6/6 可达）+ equiv；L2：科技页 6 节点可渲染；curve-sim：6/6 研完、升级仍 42 | `CFG.tech.longLadder=false` |
| 6 | **免维护带** ✅**已完成** | S4 | `config.js upkeep`、`math.js@322`（armyCount/freeBandSize/upkeepBandFactor） | 中 | L1：B01-B03 六点边界逐值 + equiv；curve-sim：带兵 10h 余粮 +0.28/s（ON 54330 vs OFF 44381） | `CFG.upkeep.freeBand=false` |
| 7 | **人口结构对齐** ✅**已完成** | S5 | `config.js pop`、`math.js@59-61`（popGrowthPer10s/popCurrent）、`ui.js@28/78` | **高** | L1：C01-C03（增长/封顶/新增分配受限/不回收既有）+ equiv；L2：主页村民显示；curve-sim：仅影响开局 10s、长线不变 | `CFG.pop.growth=false` |
| 8 | **曲线定参复跑**（骨架拟合度验收） | S6 | `tests/ie001/curve-sim.js`、`config.js` | 低 | L1：墙位落进三阶段预算（0-3h 爽 / 3-15h 墙 / 15-50h 通胀）；输出目标值候选表 | 无需回滚（只改测试/参数候选） |

### M3 数据与幂等
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证 | 回滚 |
|---|---|---|---|---|---|---|
| 9 | **存档 v3 骨架** ✅**已完成** | B1 | `math.js@431-500`（targetSaveVersion/serializeSave/validateSave/migrateSave/applySaveToS）、`config.js save` | 中高 | L1：F01-F06（迁移/幂等/坏字段保护/往返/关闭=v2/未来版本拒收）+ equiv（关闭＝与 HEAD 逐字节相同）；L2：写出 v3+骨架字段；**L4 待部署** | `CFG.save.v3=false` |
| 10 | **幂等层** ✅**已完成** | B2 | `config.js idem`、`math.js`（幂等原语 + researchScience/commitSaveData/restoreBackupByText）、`technology.js upgradeUnit` | 中 | L1：G01-G05（只扣一次/窗口外可重/环形上限/重复导入幂等）+ equiv；L2：开关断言含 idem | `CFG.idem.enabled=false` |

### M4 离线闭环（验收映射的端到端）
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证 | 回滚 |
|---|---|---|---|---|---|---|
| 11 | **离线结算 + 报告卡** ✅**已完成** | B3 | `config.js offline`、`math.js`（结算模块）、`ui.js`（启动/回前台/报告卡） | 中 | L1：H01-H07（Δ/起结线/24h/回拨/断粮/幂等/清空）+ equiv；**L2：端到端 6 条**（报告卡 3600s、入账、DOM 可见、落库、清空、重载不重复）+ 截图；**L3 未运行（无设备）** | `CFG.offline.enabled=false` |
| 12 | **离线推进（队列/建筑闭式）+ 驻军冻结** ✅**已完成** | B4 | `math.js`（advanceBuildingsBy/offlineAdvanceSec）、`ui.js` 无改 | 中高 | L1：J01-J06（闭式完成/按实际时长扣减/队列产出/驻军冻结/开关关闭/闭式 vs 逐秒对拍）+ equiv；L2：40/40+28/28 | `CFG.offline.advance=false` |

### M5 经济对齐与收尾
| # | 切片 | 改造点 | 范围 | 风险 | 实机验证 | 回滚 |
|---|---|---|---|---|---|---|
| 13 | **交易所多汇率 + 日限 + 损失** ✅**已完成** | C3 | `config.js market`、`math.js`（localDay/dailyCount/bumpDaily/exchangeResource）、`ui.js@383` | 中 | L1：K01-K05（10 汇率/无套利/日限/跨日重置/关闭无日限/未知拒绝）+ V05 + equiv；L2：面板与端到端仍绿 | `CFG.market.multiRate=false` |
| 14 | 科技收入改占人口学者制 | C1 | `config.js`、`math.js`、`ui.js` | **高** | L1：占人口断言、产出=人数×速率 ±2%；L2：分配后人口/产出一致 | 开关 `CFG.tech.occupyPop=false` |
| 15 | 被动建筑人口约束 + 停产标识 | C2 C4 | `config.js@280-288`、`math.js@529`、`ui.js` | **高（C2）/低（C4）** | L1：无人口时按规则停产；L2："原料不足已停产"可见 | 开关 `CFG.passive.needPop=false` |
| 16 | 收尾：版本纪律 + 配置化边界表 + 文档同步 | D1 D2 D3 | `index.html`、文档 | 低 | L4：`?v=N` 生效；文档审查 | revert |

## §1 里程碑与验收映射
```
M0 推演基线(1✅) → M1 基建(2) → M2 骨架拟合(3-8) → M3 数据幂等(9-10) → M4 离线闭环(11-12) → M5 经济收尾(13-16)
```
| 验收项（无服务端版） | 落点 | 证据 |
|---|---|---|
| 启动加载→离线结算→操作→落库 端到端 | 9+10+11+12 | L2 全链命令/输出/截图 + 存档 diff |
| 篡改本地时间结算有界可对拍 | 11 | L1/L2 前跳/回拨用例输出 |
| 重复操作不重复发奖 | 10 | L2 连点/重复导入用例（只扣 1 次） |
| 回归全绿 | 每片 | L1 32 + L2 33/28，贴命令与退出码 |
| 回滚实际演练一次 | M3 后 + M4 后 | 回滚前后 `git rev-parse` + 回归结果 |

## §2 骨架拟合验收口径（新增，对齐用户指令）
- **骨架达标判据**：切片 8 复跑后——①三条线（资源/人口/科技）触顶时刻与阶梯形态**落进三阶段预算**（0-3h 爽 / 3-15h 墙 / 15-50h 通胀）；②解锁链**无死锁**（每道墙后可达下一门）；③免维护带/离线/日界/上限空间与竞品**同构**
- **皮的边界**：不改资源名/兵种/关卡/Boss/UI 风格；仅数值与结构形态
- 特色发展（差异化机制）**不在本轮**，待骨架完成后再议

## §3 本计划不含
服务端/依赖/中间件；按比例灰度（用开关替代）；伤害/Boss/关卡数值改动；`math.js` 结构性重构
