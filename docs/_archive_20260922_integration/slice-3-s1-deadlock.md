# 切片 3 报告 · S1 解死锁（资源科技去战功 + 可达性检查）

## 1. 切片名与范围
- 切片：**S1 资源科技去战斗依赖 + 可达性**（`分期计划.md` 切片 3；`改造点清单.md` S1）
- 目标：解除"纯挂机 → 战功恒 0 → `sci_iron` 不可研 → 冶炼厂/铸币厂/市场/兑换全部不可达"的死锁链
- 改动文件（**仅 2 处生产代码 + 测试/工具**）：
  - `config.js`：`CFG.tech.sciencesNoMerit: false → **true**`（启用；回滚＝改回 false）
  - `math.js`（`researchScience`）：开关启用时**运行时豁免**资源科技的战功消耗；**原始配置数值不动**（`sci_iron.cost.merit=10` 等仍保留=皮/数值未改）
  - `tests/ie001/run.js`：新增 X01-X03；更新 V08/W01（见 §7）
  - 新增 `tools/verify/reachability.js`（可达性证据工具）
- 回滚方式：`CFG.tech.sciencesNoMerit=false`（立即恢复改造前行为，X02 已证）

## 2. L1 验证
```
node --check config.js / math.js / tests/ie001/run.js / tools/verify/reachability.js   → exit 0
node tests/ie001/run.js                                                                → 通过 39 / 失败 0，exit 0
  [PASS] X01 战功=0 可研完资源科技链（开关启用）
  [PASS] X02 开关关闭时恢复旧行为（战功不足拦截 + 照常扣战功）
  [PASS] X03 可达性：前置图完整无环，且每个解锁建筑研究后可建（墙后有门）
node tools/verify/reachability.js                                                      → exit 0
  资源科技 3/3 可达（纯挂机）：sci_copper/sci_iron/sci_coin 战功生效值均为 0
  建筑门：mine/smelter/mint/market = 科技门（可达）；stable/mage_tower = 击杀门（战斗源）
  兵种研究门：24 个节点 100% 依赖战功/精魄（战斗源）→ 属设计预期，不在 S1 范围
```

## 3. L2 验证
```
node tests/ie001/browser_smoke.js    → 通过 34 / 失败 0，exit 0
node tests/ie001/browser_interact.js → 通过 28 / 失败 0，exit 0
```
截图：本片改的是研究门槛与资源链，**未改任何界面结构** → 未新增截图（研究报告卡/面板类截图留给切片 11/13）

## 4. L3 / L4 状态
- **L3 未运行（无设备）**；**L4 未部署**（按纪律：实现+本地验证完成后再统一部署，避免半成品上线）

## 5. curve-sim 前后对比（关键证据：死锁是否真的解开）
| 指标（10h，默认均衡） | 改动前 | 改动后 |
|---|---|---|
| 已研资源科技 | 仅 `sci_copper@10s` | **`sci_copper@10s, sci_iron@30s, sci_coin@70s`** |
| 经济建筑 | mine lv1；冶炼/铸币/市场 **未建** | **mine/smelter/mint/market 全部 lv1** |
| 铁 / 金币 | **0 / 0** | **1900 / 6000**（铜 0＝全部投产为铁） |
| 食物 | 46800 | **1.71**（铸币厂 5 粮/s/级 吃掉大部分） |
| 拒绝统计首位 | `需升级城镇到Lv.2×27002`、`战功不足×3597` | `需升级城镇到Lv.2×32175`、`资源不足×7391`（**战功不足已消失**） |

**结论：死锁已解**（战功不足不再出现在拒因前列；冶金/铸币/市场链路全部打通）。
日志：`logs/slice3-baseline-curve-sim-10h.log`、`logs/slice3-after-curve-sim-10h.log`

## 6. 阈值达成情况
| 阈值 | 结果 |
|---|---|
| 战功=0 时资源科技 3/3 可达 | ✅ X01 + curve-sim（sci_iron@30s/sci_coin@70s） |
| 每道墙后有门（解锁建筑可建） | ✅ X03（研究后 4 座建筑全部可建） |
| 前置图完整无环 | ✅ X03 |
| 开关关闭即回旧行为 | ✅ X02（拦截 + 照常扣战功 10） |
| 回归全绿 | ✅ L1 39/39、L2 34/34、28/28 |
| 原始数值未被修改 | ✅ `sci_iron.cost.merit=10` 原样保留（仅运行时豁免） |

## 7. 断言更新说明（**按纪律披露：改了什么断言、为什么**）
| 用例 | 原断言 | 新断言 | 原因 |
|---|---|---|---|
| `V08` | 研完 `sci_iron` 后 `merit===90`；`sci_coin` 后 `merit===70` | `merit===100`（不扣） | 切片 3 已启用 `sciencesNoMerit`；**旧行为路径未被删除**——`X02` 专门验证"开关关闭时照常扣 10 战功" |
| `W01` | `tech.sciencesNoMerit===false`（全部开关默认关） | `sciencesNoMerit===true`（切片 3 故意启用），其余开关仍断言默认关 | 同上；避免把"已启用的既定变更"写成"默认关闭" |
> 未删除任何待验证行为：旧行为由 X02 独立覆盖；V08 的链路/前置/重复拒绝断言全部保留。

## 8. 回退验证
- 回滚步骤：`config.js` 中 `CFG.tech.sciencesNoMerit=true` 改回 `false` → 立即恢复旧行为（X02 为静态证据）
- 彻底回退：`git checkout -- config.js math.js`
- **未实际执行回退**（本片无失败；回滚演练按计划在 M3/M4 后各一次）

## 9. 结论
**通过**（L1 ✅ / L2 ✅ / 死锁已解 ✅ / 可达性 ✅ / 阈值全部达成；L3 未运行-无设备、L4 未部署）

## 附：本片暴露的新观察（供后续切片，未擅自处理）
| # | 观察 | 影响切片 |
|---|---|---|
| O1 | 铸币厂 `5 粮/s/级` 使食物近乎归零（10h 后 1.71） | 切片 13（交易所）/ 切片 6（免维护带） |
| O2 | 新建筑与升级争资源 → 仓库停在 lv1（上限 20000，比改动前 60000 更低） | 切片 4（上限扩容）/ 切片 8（定参） |
| O3 | `需升级城镇到Lv.2×32175` 成为**唯一最大阻塞**（F3/R2 未解） | 切片 4/8（是否给非战斗 caps 通道，待你裁决） |
