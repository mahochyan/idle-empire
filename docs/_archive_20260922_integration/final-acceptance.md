# 总验收报告 · 策划基线整合 + 放置时代「本地等价」改造

- 日期：2026-09-22｜范围：阶段 0-5（只读侦察 → 文档整合 → 规格 → 计划 → 分片实施 → 迁移与回滚演练）
- **结论**：**批准计划内的可实施切片全部完成并逐片验证**；R2=A（含用户新指令「加入地契」）已落地；剩余 2 个切片与 2 项数值对齐**等你裁决**（下表 §5）

---

## 1. 交付物清单

### 1.1 代码与配置（未提交，可回滚）
| 文件 | 变更 |
|---|---|
| `config.js` | 新增/扩展：`CFG.offline`、`idem`、`caps(+expand/ownMax)`、`upkeep`、`tech(sciencesNoMerit/longLadder)`、`food`、`pop`、`unitCapBoost`、`save.v3`、`market(multiRate/dailyLimit/12 汇率)`、`townGate(地契)`、`res.deed`、`sciencesLong`、`diag` —— **全部为开关/参数，原始数值一律保留** |
| `math.js` | 新增：`logDiag` 家族、幂等原语、离线结算（`offlineNetRates/offlineDeltaSec/settleOffline/dismissOfflineReport`）、离线推进（`advanceBuildingsBy/offlineAdvanceSec`）、存档 v3（`targetSaveVersion/SAVE_V3_KEYS`）、`capsExpanded/ownMaxFor`、`upkeepBandFactor/armyCount`、`popCurrent/popGrowthPer10s`、`localDay/dailyCount/bumpDaily`、`townGateCost/townGateShortfall`；`tick()` 的计时块**等价抽出**为 `advanceBuildingsBy(secs)` |
| `ui.js` | 离线报告卡、启动/回前台结算触发、村民与地契需求展示、市场剩余次数、消耗显示同源 |
| `technology.js` | 科技表走 `activeSciences()`；`upgradeUnit` 幂等 |
| `sprites.js` | 新增 `deed` 像素图标 |
| `index.html` | 加载器增加 `APP_VERSION/APP_SCRIPTS` 自检锚点（`?v=` 纪律不变） |

### 1.2 文档（`docs/codex/plan/`）
`策划基线-v1.md`（§11 实施进度）｜`冲突清单.md`（C1-C6 全裁决）｜`待补缺口清单.md`（G1-G9）｜`可迁移机制规格-v1.md`（S1-S8 + 裁决表）｜`实机验证标准.md`（L1-L4 + 阈值）｜`改造点清单.md`｜`分期计划.md`｜`发布检查清单.md`｜`数值配置化边界表.md`｜`决策备忘录-R3R5.md`｜`决策备忘录-地契.md`｜`答复-R2-放置时代进度门.md`｜`候选-S2科技阶梯重定时.md`｜`reports/backend-recon.md`

### 1.3 测试与工具
- 测试：`tests/ie001/run.js`（**96 例**）、`browser_smoke.js`（**41 例**）、`browser_interact.js`（**28 例**）、`curve-sim.js`（推演，含 `--only/--cost/--scicost/--techcap/--nofreeband` 沙箱旋钮）
- 工具：`equiv.js`（零行为对拍）、`reachability.js`（可达性）、`probe-offline.js`（最小复现）、`config-boundary-audit.js`、`edge-reaper.js`（**浏览器实例清理机制**，防内存泄漏）

---

## 2. 验收标准逐条对照

| 验收项 | 状态 | 证据 |
|---|---|---|
| 五份产出齐全，冲突每条有结论或标注待裁决 | ✅ | 基线/冲突（C1-C6 已裁决）/规格/清单/计划；+实机验证标准、发布清单、边界表 |
| **端到端：登录→拉取存档→离线结算→操作→落库** | ✅ | L2 六条断言：报告卡 3600s → 增益入账（木+4320）→ DOM 可见 → 主档持久化 → 清空 → 重载不重复；截图 `assets/slice11-offline-report-360.png` |
| **篡改本地时间结算有界且对拍可证** | ✅ | H03（3 天 → 截断 86400s）、H04（ts 在未来 → 拒绝、资源不变且不为负）、H02（119/120s 起结线） |
| **同一请求重复提交不重复发奖** | ✅ | G01（兵种研究只扣 1 次）、G02（资源科技 repeat 不扣）、G05（同文本重复导入不二次覆盖、PRE 不重写） |
| **回归全绿** | ✅ | L1 **96/96**、L2 **41/41 + 28/28**（命令与退出码见各切片报告与日志） |
| **回滚步骤实际执行过一次** | ✅ | 四步演练：备份→`git checkout` 回 HEAD→回滚态 L1 32/32 + L2 33/33→恢复→L1 96/96 且对拍一致（`slice-12` 报告） |
| 零行为守护（所有开关关闭 ⇒ 与 HEAD 一致） | ✅ | `equiv.js`：存档剔 ts **逐字节相同**（每片复跑） |
| 不新增依赖/服务、不删老接口、不提交密钥 | ✅ | 无 `package.json` 依赖变更、无服务端、无 `.env`/密钥；存档键与旧接口保留 |
| 每阶段停下汇报、未获确认不跨阶段 | ✅ | 19 轮逐片汇报；未实施的 2 片与 2 项数值始终标注"待裁决" |

---

## 3. 已实施切片（19 项，均带开关与逐片证据）

| 切片 | 内容 | 开关（默认） | 关键证据 |
|---|---|---|---|
| 1 | curve-sim 推演脚本 | — | 34 锚点、确定性复跑零差异 |
| 2 | 诊断环形日志 + 开关骨架 | `diag`(on) | W01-W04 + 零行为对拍（首次建立） |
| 3 | 资源科技去战功（**解死锁**） | `tech.sciencesNoMerit`(true) | 可达 6/6；铁/金币从 0 → 1900/6000 |
| 4 / 8a / 8b | 上限扩容 → 单位上限 → 自身 LvMax | `caps.expanded/unitCapBoost/ownMax`(true) | 城镇门拒绝从 top5 消失；带兵 140 触发免维护罚息 |
| 4b | 食物经济对齐（R5-①②） | `food.aligned`(true) | 升级启动 **0→42**、食物 −2.74→+2.79/s |
| 5 | 科技长阶梯（3→6 节点） | `tech.longLadder`(true) | 6/6 研成、节奏 30s→2530s |
| 6 | 军粮免维护带 + 分段斜率 | `upkeep.freeBand`(true) | 六点边界逐值；余粮 +0.28/s |
| 7 | 人口结构（派生式，不新增字段） | `pop.growth`(true) | 基础 4 起步，长线不变 |
| 8 | 曲线定参复跑 | — | 三阶段拟合判定 + 根因 G1-G6 |
| 9 | **存档 v3**（ops/offline/daily） | `save.v3`(true) | 迁移矩阵 + 坏字段保护 + v4 拒收 |
| 10 | **幂等层** | `idem`(true) | 只扣一次 + 窗口外可重 + 环形上限 |
| 11 | **离线结算 + 报告卡** | `offline.enabled`(true) | L2 端到端 + 截图；**修复木/石净速率为 0 的真 bug** |
| 12 | **离线推进** + 驻军冻结 | `offline.advance`(true) | 闭式 vs 逐秒对拍；**修复 off-by-one** |
| 13 | **交易所多汇率 + 日限 + 价差** | `market.multiRate`(true) | 12 汇率无套利、日限 5 次、跨日重置 |
| **S8c** | **加入地契 + 城镇门改「地契+科技」** | `townGate.useDeed`(true) | 村民 10→**40**、升级 49→**100**、食物 +3.79→**+37.65/s** |
| 16 | 收尾（版本纪律/边界表/文档同步） | — | P01 自检 + L2 版本锚点 |

---

## 4. 关键修复（本轮抓出的真缺陷，均已回归）
1. **木/石离线净速率为 0**（`type==='basic'` 判据错误 → 探针复现 → 改判据）—— 切片 11
2. **离线推进 off-by-one**（完成通道多扣 1 秒 → 闭式 vs 逐秒对拍锁死）—— 切片 12
3. **学院扩容分支未短路**（lv19 被 barracks 回退误锁）—— 切片 4
4. **推演/分析工具口径缺陷 2 处**（食物耗粮用原始值而非生效值；H01 期望值自证）—— 切片 4b/11
5. **浏览器实例泄漏**（280 进程 / 24.7 GB → 建成 `edge-reaper` 清理机制并实测：正常跑 0 残留、强杀后 CLI 可清）

---

## 5. 未实施项（**等你裁决**，均已在文档中给出候选与影响）

| # | 项 | 为什么等 | 候选/影响 |
|---|---|---|---|
| 1 | **切片 14** 科技收入改占人口学者制（C1/C2 裁决） | 会改变经济核心；且与城镇门/人口上限强耦合（现已由 S8c 打开，可做了） | 需你确认"学者占人口"的具体形态（新增轨道 or 改造学院） |
| 2 | **切片 15** 被动建筑人口约束（C5 裁决）+ 停产标识 | 同上 | 需确认占人口数/停产规则 |
| 3 | **S2 科技阶梯重定时** | 改研究节奏 | 候选 **×80 + 科技上限 ×20**：节点落 0.29h/0.68h/1.33h/2.29h/4.16h/**16.2h**（补上 3-15h 中期墙） |
| 4 | **S9 成本剖面换竞品分段线性** | 改升级成本曲线 | 50h 实测：采集建筑 lv9/7/9（vs 现 3/3/2）、消除后期食物枯竭、升级 98→117 |
| 5 | 地契命名/汇率/成本曲线确认 + Boss 可选加速 | 你点名"地契"但未定细节 | 现用「地契」、100 金币→1、地契 5…520 + 科技 0…12000；Boss 加速**未实现** |

## 6. 已知风险与回滚路径
| 风险 | 缓解 |
|---|---|
| 改动量大（9 个文件、19 个切片） | **每片独立开关**；`equiv` 守护"全开关关闭=与 HEAD 逐字节相同" |
| v3 档被旧代码读会丢 `ops/offline/daily` | 已在 `slice-9` 明确声明**不承诺完全兼容**；回滚前建议导出存档 |
| 数值对齐未定（S2/S9） | 未实施；`curve-sim` 沙箱可无风险试算 |
| **L3 真机未运行** | 无 Android 设备；所有真机项标"未运行"，**不用视口模拟冒充** |
| 机器负载 | `edge-reaper` 每次运行自证 0 残留；长任务改后台并即时清理（本次实测 Edge 稳定 24 进程 / 2.67 GB） |

## 7. 诚实声明
- 所有数值（地契曲线、长阶梯成本、免维护带段宽、食物对齐等）均为**候选值**，标注在文档中；只有你明确裁决过的（R5-①②、O8、R2=A、地契）才被启用
- 未运行层级（L3 真机、L4 线上部署）**一律标注"未运行"**，未以任何形式冒充通过
- 本轮**未提交、未推送**：改动全部留在工作区（`git status` 9 个已跟踪文件 + 新增文档/工具），便于你审查或回滚
