# 切片 4b 报告 · R5 食物经济对齐（用户裁决 B：铸币耗粮 5→1 + 食物产出 0.75→2.25）

## 1. 切片名与范围
- 来源：`决策备忘录-R3R5.md` 灵敏度分析 → 用户裁决 **B（R5-①②）**
- 改动（4 个文件）：
  - `config.js`：`CFG.food = { aligned:true, res:{food:{basePerPop:2.25}}, consumes:{mint:{food:1}} }` —— **原始数值全保留**（`CFG.res.food.basePerPop=0.75`、`CFG.buildings.mint.consumes.food=5` 未改）
  - `math.js`：新增 `alignedResBase(rk)` / `effConsume(key,res)`；`prodRate()` 与 `passiveProduction()` 改用运行时生效值
  - `ui.js`：建筑卡"消耗"显示改用 `effConsume`（**展示口径与实现同源**，防 02-C1 假口径事故）
  - 测试/工具：新增 Z01-Z03；`equiv.js` 关闭清单加入 `CFG.food.aligned`
- 回滚方式：`CFG.food.aligned=false`（Z02/Z03 证明回到原始 0.75/5 且可逆无残留）

## 2. L1 验证
```
node --check config.js/math.js/ui.js/tests/ie001/run.js → exit 0
node tests/ie001/run.js                                 → 通过 45 / 失败 0，exit 0
  [PASS] Z01 对齐后食物产出 10 村民=22.5/s；铸币 lv2 比 lv0 多耗 2/s（隔离断言）
  [PASS] Z02 开关关闭回到原始 0.75 / 5
  [PASS] Z03 开→关→开可逆；原始配置未被改写
node tools/verify/equiv.js                              → exit 0（全开关关闭 ⇒ 存档剔 ts 与 git HEAD 逐字节相同）
```

## 3. L2 验证
```
node tests/ie001/browser_smoke.js    → 通过 34 / 失败 0，exit 0
node tests/ie001/browser_interact.js → 通过 28 / 失败 0，exit 0
```
截图：无界面结构变化（仅"消耗 N/s"数字随生效值显示）→ 未新增截图

## 4. L3 / L4 状态
L3 未运行（无设备）；L4 未部署（M2 骨架完成后统一部署验证）

## 5. curve-sim 前后对比（10h，默认均衡）
| 指标 | 切片4b 前 | 切片4b 后 |
|---|---|---|
| 食物净速率(含被动耗粮) | **−2.74/s** | **+2.79/s** |
| 食物转负 | 0.21h | **未发生** |
| **升级启动次数** | **0** | **42** |
| 学院等级 | 1 | **20（触顶）** |
| 仓库 / 矿井 / 冶炼 / 铸币 / 市场 | 1/1/1/1/1 | **3/6/4/6/5** |
| 铁 / 金币 / 科技点 | 7600 / 24000 / 5000 | **12400 / 44000 / 43000** |
| 已研资源科技 | 3/3 | 3/3 |

> 与裁决前灵敏度预测（S3 情景：升级 42、学院 20、木61626/石1877/食79132/科技43000）**逐项吻合** → 决策模型可信。

## 6. 阈值达成情况
| 阈值 | 结果 |
|---|---|
| 食物净速率转正且不转负 | ✅ +2.79/s、未发生转负 |
| 升级能真正启动（骨架"用得到上限"） | ✅ 0 → 42 |
| 展示与实现同源 | ✅ ui 消耗显示走 `effConsume` |
| 关闭开关 = 原始数值 | ✅ Z02 |
| 全开关关闭 ⇒ 与 HEAD 逐字节等价 | ✅ equiv |
| 回归全绿 | ✅ L1 45/45、L2 34/34、28/28 |
| 原始配置未被改写 | ✅ Z03 断言 |

## 7. 断言更新披露
| 用例 | 原断言 | 新断言 | 原因 |
|---|---|---|---|
| `V04` | `S.res.food < 1000`（食物被铸币 5/s 吃掉） | `S.res.food !== 1000`（联动变化，不断言方向） | 对齐后食物产出 > 消耗，原方向断言失效；改为"确实发生联动"并保留其余断言（产出/上限/缺料停产/不扣负） |
| `Z01` | 首版按"1000+22.5−2"精确断言 | 改为**隔离断言**：mint lv2 vs lv0 的单 tick 差 = 2 | 首版漏算军粮/口粮；隔离法只测"对齐后的耗粮"这一待验证行为，不依赖其他消耗项 |
> 未删除任何待验证行为。

## 8. 工具修正披露（诚实记录）
- 发现**我自己的分析工具口径 bug**：`curve-sim.js` 与 `sensitivity.js` 的"食物耗粮"用**原始** `consumes.food=5` 计算，而引擎按 `effConsume=1` 运行 → 修复前 curve-sim 误报"食物 −21.21/s、转负 2.85h"
- 已修复为"与实现同源"（调用 `effConsume`），复跑后：**+2.79/s、未发生转负**；灵敏度工具复跑后 S0-S3 收敛一致（因配置已对齐，覆盖为空操作），S4（全资源 ×3）另见下

## 9. 回退验证
- 回滚：`CFG.food.aligned=false` → 立即回到 0.75/5（Z02/Z03 静态证据）
- 彻底回退：`git checkout -- config.js math.js ui.js`
- 未实际执行回退（本片无失败）

## 10. 结论
**通过**（L1 ✅ / L2 ✅ / 骨架可用性 0→42 升级 ✅ / 指标同源 ✅ / 零行为对拍 ✅）

## 附：本片带出的新观察（供后续切片）
| # | 观察 | 影响 |
|---|---|---|
| O5 | 额外把资源速率 ×3（R3，灵敏度 S4）可把升级启动从 42 提到 **55**，但改动面最大（3 个数值） | 切片 8（定参）/后续裁决 |
| O6 | 学院已在 10h 触顶 lv20（`scienceCapPerTown`），说明**科技阶梯（切片 5）已具备承载空间** | 切片 5（S2 阶梯）可继续 |
| O7 | 采集 buff 建筑（伐木场等）仍停在 lv1：升级成本高 + 资源被仓库/生产建筑分流 | 切片 5/8 定参时一并调 |
