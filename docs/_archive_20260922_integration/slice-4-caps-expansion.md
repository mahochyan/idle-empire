# 切片 4 报告 · S3 上限空间扩容（阶段 4）

## 1. 切片名与范围
- 切片：**S3 上限空间扩容**（`分期计划.md` 切片 4；骨架拟合优先项）
- 目标：对齐竞品"分级线性长堆叠"的上限空间（竞品：工坊 LvMax 50、仓库 LvMax 1000 每级 +1000 粮/+600 木/+200 知识）
- 改动（3 处生产代码 + 测试/工具）：
  - `config.js`：`CFG.caps.expanded: false → **true**`，新增 `CFG.caps.expand`（仓库 50000/级、仓库上限 城镇×20、科技建筑 城镇×20、生产建筑 城镇×10、采集 buff 建筑 城镇×10、功能建筑 城镇×10；被动/科技资源上限表）——**原始数值全部保留**（`CFG.res.*.max`、`buildings.warehouse.storagePerLv`、`buildingCaps` 未改）=皮不动
  - `math.js`：`capsExpanded()/expandedResCap()` 新helper；`storageCapacity()`、`upgradeLockReason()`、`resCap()` 按开关读取扩容值
  - **修复一处真 bug**：扩容分支命中后未短路 → 学院 lv19 继续落回 barracks 回退分支被误锁（Y02 抓到）
  - `tests/ie001/run.js` 新增 Y01-Y03；`browser_smoke.js` 更新 L2 断言；`tools/verify/equiv.js` 升级为"强制关闭全部开关后对拍"
- 回滚方式：`CFG.caps.expanded=false`（Y03 证明开→关可逆无残留）

## 2. L1 验证
```
node --check config.js / math.js / tests/ie001/run.js / tools/verify/equiv.js → exit 0
node tests/ie001/run.js                                                        → 通过 42 / 失败 0，exit 0
  [PASS] Y01 开关关闭时上限与改造前逐值一致（仓库 10000+5×10000、科技 500+200×10、铜 2000+500×1）
  [PASS] Y02 开关开启后扩容生效（仓库 10000+20×50000、科技 3000+2000×20、铜 8000+2000×10、铁 6000+1600×10）
        且等级上限放宽（仓库/学院 lv19、矿井/市场/伐木场 lv9 可达；lv20/lv10 到上限）
  [PASS] Y03 开→关可逆、无残留
node tools/verify/equiv.js                                                     → exit 0
  强制关闭全部改造开关后与 git HEAD：字段集一致 + 存档(剔 ts)逐字节相同 ✅
```

## 3. L2 验证
```
node tests/ie001/browser_smoke.js    → 通过 34 / 失败 0，exit 0
node tests/ie001/browser_interact.js → 通过 28 / 失败 0，exit 0
```
截图：本片改的是上限数值与等级闸门，**无界面结构变化** → 未新增截图

## 4. L3 / L4 状态
- **L3 未运行（无设备）**；**L4 未部署**（按纪律：M2 骨架完成后统一部署验证）

## 5. curve-sim 前后对比（上限空间是否真的打开）
| 指标（10h，默认均衡） | 切片3 后 | 切片4 后 |
|---|---|---|
| 仓库容量上限 | 20000 | **60000**（10000+1×50000） |
| 科技点上限 | 700 | **5000**（3000+2000×1） |
| 铁上限 | 1900 | **7600** |
| 金币上限 | 6000 | **24000** |
| 建造/升级统计 | — | 尝试 39599｜建成 11｜**升级启动 0** |

## 6. 阈值达成情况
| 阈值 | 结果 |
|---|---|
| 扩容数值逐值正确 | ✅ Y02（仓库/科技/铜/铁/金币 + 五类建筑等级上限） |
| 关闭开关 = 改造前逐值 | ✅ Y01 |
| 开→关可逆无残留 | ✅ Y03 |
| 全开关关闭 ⇒ 与 HEAD 逐字节等价 | ✅ equiv |
| 回归全绿 | ✅ L1 42/42、L2 34/34、28/28 |
| 原始数值未被修改 | ✅ 仅运行时按开关读取扩容表 |

## 7. 断言更新披露（改了什么、为什么）
| 用例 | 原断言 | 新断言 | 原因 |
|---|---|---|---|
| `V07` | 隐含使用当前上限式 | 显式 `CFG.caps.expanded=false` 后断言 500+200×10 | 该用例本意是验证**原始**上限口径；扩容路径由 Y01/Y02 覆盖 |
| `W01` | `caps.expanded===false` | `caps.expanded===true`（切片 4 已启用） | 本片既定行为变更 |
| `browser_smoke` L2 | `CFG.caps.expanded===false` | 断言 `sciencesNoMerit===true && caps.expanded===true`，其余开关仍关 | 同上；未删除任何待验证行为 |

## 8. 回退验证
- 回滚：`CFG.caps.expanded=false` → 立即回到改造前上限（Y01/Y03 为静态证据）
- 彻底回退：`git checkout -- config.js math.js`
- **未实际执行回退**（本片无失败）

## 9. 结论
**通过**（L1 ✅ / L2 ✅ / 扩容生效 ✅ / 可逆 ✅ / 零行为对拍 ✅；L3 未运行-无设备、L4 未部署）

## 附：本片暴露的关键约束（**阻塞骨架拟合验收**，需你裁决）
| # | 发现 | 量化证据 | 建议（未实施） |
|---|---|---|---|
| **O1′** | **食物经济把一切升级钉死**：铸币厂 `5 粮/s/级` 使食物净速率 **−2.74/s（默认）/ −0.56/s（激进）**，0.21h/0.14h 即转负；所有升级都需食物 → **升级启动 0 次**，等级永远 lv1，扩容只能"看得见上限、用不到上限" | `slice4-after-curve-sim-10h.log`（指标已修正为**含被动耗粮**） | R5 三选一：①降铸币耗粮（5→1）；②提升食物产出（农田 buff/口粮下调）；③铸币厂改为"按库存可用量运行 + UI 提示"（不耗空食物） |
| O2′ | 城镇门仍是第二大阻塞（`需升级城镇到Lv.2×3582`） | 同上 | R2（非战斗 caps 通道）仍待裁决 |
| O3′ | 推演脚本指标缺陷已修：原"食物净速率"未计入被动耗粮（误导） | 本片已修复并复跑 | — |
