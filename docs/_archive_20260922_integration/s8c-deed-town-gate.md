# S8c 报告 · 加入「地契」+ 城镇门改「地契+科技」（用户裁决 D2）

## 1. 范围（R2=A 第二半落地）
- 用户指令：**「加入地契吧」** → 采纳 `决策备忘录-地契.md` 的 **D2（新设地契资源）**
- 改动（4 个生产文件）：
  - `config.js`：新增资源 `CFG.res.deed`（地契，上限 999999）；新增 `CFG.townGate = { useDeed: true, cost:[9 档] }`（**候选值**：地契 5/15/30/60/100/160/240/360/520 ＋ 科技点 0/100/300/800/1800/3000/5000/8000/12000）；市场新增 `coin→deed 0.01`、`deed→coin 80`（往返 0.8，无套利）
  - `math.js`：`townCanUpgrade()` / `townGateCost()` / `townGateShortfall()`；`upgradeTown()` 走"地契+科技"扣费（**只扣一次**）；`migrateSave` 对缺键补 `res.deed=0`；`serializeSave` 在开关关闭时**剔除 deed 键**（保持与 HEAD 逐字节等价的守护）
  - `ui.js`：主页城镇卡显示「需地契 X · 科技点 Y（还差 …）」（**展示同源**）
  - `sprites.js`：新增 `deed` 像素图标（卷轴+朱印，32×32 风格一致）
- **原始数值保留**：`CFG.town[].needBossId` 与 10 档 `maxPop` **未改**；Boss 门作为**回滚路径**保留（开关关闭即恢复）
- 回滚：`CFG.townGate.useDeed=false`

## 2. L1（命令/输出/退出码）
```
node --check config.js/math.js/ui.js/sprites.js/tests/ie001/run.js → exit 0
node tests/ie001/run.js                                            → 通过 96 / 失败 0, exit 0
  [PASS] L01 地契资源存在、上限 999999、旧档迁移补 0 且**不改动既有资源**
  [PASS] L02 金币→地契：1000 金币 → 10 地契；往返 0.01×80=0.8 <1 无套利
  [PASS] L03 城镇门＝地契+科技：不足时拒绝**且不扣费**（即使 Boss 已击杀）；缺口提示含地契/科技点
  [PASS] L04 Lv1→Lv2：扣地契 5（科技门槛 0）只扣一次，计时结束升到 Lv2
  [PASS] L05 关闭开关 → 回到 Boss 门；**存档不含 deed 键**
  [PASS] L06 Lv2→Lv3 按表扣费（地契 15 + 科技 100）
node tools/verify/equiv.js                                         → exit 0（全开关关闭含 townGate ⇒ 与 HEAD 逐字节相同）
```

## 3. L2（真实浏览器）
```
node tests/ie001/browser_smoke.js    → 41/41 exit 0
node tests/ie001/browser_interact.js → 28/28 exit 0
清理核对：两套件 headless 残留 0
```

## 4. 效果实证（curve-sim 10h，推演策略加入"买地契→升城镇"）
| 指标 | S8c 前（切片8b 末态） | **S8c 后** |
|---|---|---|
| 村民 | `4/3/3` = **10**（城镇 Lv1） | **`16/12/12` = 40**（城镇 **Lv4**） |
| 10h 木 / 石 / 食 | 57355 / 1416 / 106305 | **248206 / 12463 / 259989** |
| 食物净速率 | +3.79/s | **+37.65/s** |
| 升级启动次数 | 49 | **100** |
| 建筑终态 | 步兵营18/学院18/矿井5/冶炼2/铸币5/市场4/仓库2 | **步兵营27/学院27/矿井11/冶炼9/铸币11/市场9/仓库5** |
| 科技点 | 39000 | 57000（触上限） |

**判读**：城镇门从"击杀 Boss"改为"资源+科技"后，**非战斗进度通道成立**——人口上限 10→40 解锁，产出与升级规模成倍抬升，这正是骨架拟合里最缺的一环（O3′/O10/O11 全部打开）。

## 5. 阈值达成
| 阈值 | 结果 |
|---|---|
| 地契为非战斗可得（市场兑换） | ✅ L02 + 推演（卖金币买地契→升城镇） |
| 城镇升级只扣一次 | ✅ L04/L06 |
| 不足时拒绝且不扣费 | ✅ L03 |
| **不静默裁剪历史数据** | ✅ L01（旧档只补 `deed:0`，既有资源不动）；L05（关开关不写 deed 键） |
| Boss 门可回滚 | ✅ L05 |
| 展示与实现同源 | ✅ 主页显示需求与缺口 |
| 回归全绿 | ✅ L1 96/96、L2 41/41 + 28/28 |
| 零行为守护（开关关） | ✅ equiv 逐字节相同 |

## 6. 断言口径更新披露（3 处）
| 用例 | 变更 | 原因 |
|---|---|---|
| `S02` | 资源集合期望加入 `deed:0` | S8c 起旧档迁移会补地契键；**仍逐键校验既有资源不得改动** |
| `K01` | 汇率条数 10 → 12 | 市场新增地契两条（仍全配对校验无套利） |
| `L03`（本片新增） | 首版断言要求 Lv2 缺口同时含"科技点" | 我的断言写错：Lv2 科技门槛为 0；已改为 Lv2 只查地契、另测 Lv3 查两者 |

## 7. 待你确认的候选参数（不影响功能，随开关可调）
1. **地契命名**：现用「地契」（与竞品同义；如需"拓荒令/领地凭证"改名只动 `CFG.res.deed.name`）
2. **汇率**：现为 100 金币 → 1 地契（`coin→deed 0.01`）
3. **成本曲线**：现为地契 5/15/30/60/100/160/240/360/520 + 科技 0/100/300/800/1800/3000/5000/8000/12000（**候选·待推演**）
4. **Boss 可选加速**：目前**未实现**（Boss 仅作回滚路径）；若你要"击败 Boss 减免地契"，说一声我加

## 8. 结论
**通过**。R2=A 两半（上限模型 + 城镇资源门）**全部落地**，非战斗进度通道打通，效果已在 10h 推演中量化。
