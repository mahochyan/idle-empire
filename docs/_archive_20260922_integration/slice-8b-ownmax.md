# 切片 8b 报告 · 上限改自身 LvMax（R2=A 第一半）

## 1. 范围
- 来源：用户裁决 **R2=A**（对齐竞品：城镇升级改资源+科技门；**建筑/单位上限改自身 LvMax 模式**）
- 本片只做**不依赖地契决策**的那一半：`CFG.ownMax.enabled=true`，建筑等级上限改自身 LvMax（对齐竞品实测：仓库 `LvMax=1000`、工坊/学院/剧场类 `LvMax=50`）
- 改动：`config.js`（`CFG.ownMax`）；`math.js`（新增 `ownMaxFor()`，`upgradeLockReason()` 命中即短路）；测试 E01-E03；`equiv.js` 关闭清单
- 回滚：`CFG.ownMax.enabled=false`

## 2. L1（命令/输出/退出码）
```
node --check config.js/math.js/tests/ie001/run.js → exit 0
node tests/ie001/run.js                          → 通过 60 / 失败 0，exit 0
  [PASS] E01 关闭时仍为"城镇×k"（仓库 lv20 锁、lv19 可达）
  [PASS] E02 开启后按自身 LvMax：仓库1000｜训练50｜学院50｜生产50｜采集50｜功能50｜营帐10（逐项边界）
  [PASS] E03 资源/科技/击杀门仍生效（smelter 未研 sci_iron 不可建）；开→关可逆
node tools/verify/equiv.js                       → exit 0（全开关关闭 ⇒ 与 HEAD 逐字节相同）
```
断言口径修正披露：切片4 的 `Y01/Y02` 增加 `CFG.ownMax.enabled=false` 以隔离（其本意是验证"扩容路径"，未被删除）。

## 3. L2（真实浏览器 + 清理机制）
```
node tests/ie001/browser_smoke.js    → 34/34 exit 0（[reaper] 预扫 0 → 清理核对 0 残留）
node tests/ie001/browser_interact.js → 28/28 exit 0
```

## 4. curve-sim 前后对比（10h）
| 指标 | 切片8a 前 | 切片8b 后 |
|---|---|---|
| **`需升级城镇到Lv.2` 拒绝** | **×7200（第 2 位）** | **已从 top-5 消失** |
| 拒因前 5 | 资源不足 / **城镇门** / 需先研究 | 资源不足×38725 / 需先研究×508 / 科技点不足×253 / 需先研究×123 / ×111 |
| 建筑终态（默认） | 几乎全 lv1（仓库3/矿井6/冶炼4/铸币6/市场5/学院20） | **步兵营18｜学院18｜矿井5｜铸币5｜市场4｜仓库2｜营帐3** |
| 升级启动 | 42 | **49** |
| **带兵策略** | 兵力 ~35 上限、军粮罚息不触发 | **兵力 140｜军粮 8.76/s｜食物净 −0.97/s（罚息段触发）** |

**判读**：城镇门对"建筑/单位上限"的硬绑定已解除 → 骨架的两大卡点（O3′/O8）同时打开：
- 建筑能持续升级（不再被城镇等级锁死）
- 单位上限提升后 `兵力 140` 越过免维护带 → **军粮分段罚息可见**（竞品式"扩军压力"成立）

## 5. 阈值达成
| 阈值 | 结果 |
|---|---|
| 上限不再绑定城镇等级 | ✅ E02 + curve-sim（城镇门拒绝消失） |
| 取值对齐竞品 LvMax | ✅ 仓库 1000／工坊类 50 |
| 资源/科技/击杀门不被绕过 | ✅ E03 |
| 关闭开关＝原样 | ✅ E01 |
| 全开关关闭 ⇒ 与 HEAD 逐字节等价 | ✅ equiv |
| 回归全绿 | ✅ L1 60/60、L2 34/34、28/28 |

## 6. 回退
`CFG.ownMax.enabled=false` → 立即回到"城镇×k"（E01/E03 静态证据）；彻底回退 `git checkout -- config.js math.js`

## 7. 结论
**通过**。R2=A 第一半（上限模型）完成；**第二半（城镇升级门：资源+科技）等待"地契"实现方式裁决**（见 `决策备忘录-地契.md`）。

## 附：剩余观察
| # | 观察 | 说明 |
|---|---|---|
| O12 | 采集 buff 建筑（伐木场/采石场/农田）仍停 lv1 | 升级成本（upBase×upCostLv^lv）在该资源速率下偏高 → 属切片 8（S6 定参）范围 |
| O13 | 步兵营 18 / 学院 18 已可升，但**资源不足×38725** 成为首要约束 | 说明约束已从"门"转为"资源"，符合竞品"资源成本墙"形态 |
