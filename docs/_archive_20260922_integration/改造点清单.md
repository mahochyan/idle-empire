# 改造点清单 v1（阶段 3 产出 · 未实施）

- 前提：纯静态 + 本地等价实现（无服务端、不新增依赖）；依据 `可迁移机制规格-v1.md`（S1-S8 已定案）与 `冲突清单.md`（C1-C6 已裁决）
- 验收层：每项均按 `实机验证标准.md` 标注归属层（L1 node / L2 真实浏览器 / L3 真机 / L4 线上）
- 工作量量级：**小**（≤0.5 轮）/ **中**（1-2 轮）/ **大**（3-4 轮）；"轮"=本会话一次交互批次（不虚构人日精度）

## S 骨架拟合（2026-09-22 用户决策：骨架拟合竞品、保留我方皮；依据切片 1 发现 F1-F4）
| 编号 | 改造项 | 竞品依据（可复核） | 我方现状 | 影响模块 | 风险 | 依赖 | 量级 | 验证层 |
|---|---|---|---|---|---|---|---|---|
| S1 | ~~**资源科技去战斗依赖 + 可达性**~~ **✅ 已完成（切片 3，2026-09-22）**：`CFG.tech.sciencesNoMerit=true` 运行时豁免战功（原始数值未改）；新增 `tools/verify/reachability.js`；X01-X03 用例。死锁已解（sci_iron@30s/sci_coin@70s、四建筑全建成、铁1900/金币6000） | 竞品科技仅耗**知识**（450005-450025 Need=知识/粮/木，无战功）；`02 C2`"墙后必须有门" | `sci_iron` 需 10 战功；纯挂机战功恒 0（`战功不足×17997`）→ 死锁链 | `config.js@347`、`math.js@574` | 中 | 无 | 中 | L1+L2 |
| S2 | **成本阶梯扩节点对齐**：**✅ 已完成（切片 5，2026-09-22，本轮范围）** —— 节点 3→6（探矿100/冶铜300/冶金800/冶铁1800/铸币5000/货币12000，链式前置），原始表保留；A01-A03 + equiv 全绿；curve-sim 6/6 研完且升级仍 42。**剩余**：跨度 11 个数量级需"时代内容"支撑（见 slice-5 报告附） | 竞品 `developScienceList` 40+ 节点、200 → 1e13 | 资源科技 3 项（10/80/200，2 个数量级） | `config.js sciencesLong`、`math.js activeSciences`、`technology.js@243`、`ui.js@290` | 中 | S1 | 大 | L1+L2 |
| S3 | ~~**上限空间扩容**~~ **✅ 已完成（切片 4，2026-09-22）**：`CFG.caps.expand` 运行时扩容（仓库 50000/级·上限城镇×20、科技 3000+2000/级、被动资源表、五类建筑等级上限）；修复"扩容分支未短路致学院误锁"真 bug；Y01-Y03 + equiv 全绿。**新阻塞 O1′**：食物净速率 −2.74/s 致升级启动 0 次（见 slice-4 报告，待裁决 R5） | 竞品工坊 `LvMax=50`、仓库 `LvMax=1000`（每级 +1000 粮/+600 木/+200 知识） | 仓库 lv≤50、生产建筑 lv≤10（城镇×1）、科技上限 700、铜 2500 | `config.js caps`、`math.js@72/77/534` | 中 | 无 | 中 | L1+L2 |
| S7 | ~~**R5 食物经济对齐**（用户裁决 B：铸币耗粮 5→1 ＋ 食物产出 0.75→2.25）~~ **✅ 已完成（切片 4b，2026-09-22）**：`CFG.food.aligned` 运行时对齐（原始数值保留）；展示同源（`effConsume`）；Z01-Z03 + equiv 全绿；**升级启动 0→42、学院 lv20、食物 +2.79/s 未转负** | 竞品食物由牧场工人 5/s/人产出、铸币链耗粮按级 | 食物净速率 −2.74/s、升级启动 0 | `config.js food`、`math.js@42/559`、`ui.js@286` | 中 | S1/S3 | 小 | L1+L2 |
| S4 | ~~**免维护带**（IE-006 A2 落地）：军粮前 N 兵豁免、之后分段斜率~~ **✅ 已完成（切片 6，2026-09-22）**：`CFG.upkeep.freeBand` 运行时倍率（免维护带 = 10+2×营帐等级；超出按段宽 [20,80,200]×斜率 [1,2,4,8] 边际计费）；B01-B03 逐值断言 + equiv 全绿；带兵策略 10h 余粮 +0.28/s。**观察 O8**：当前单位上限仅 35 兵，罚息段几乎不触发 | 竞品 ≤200 兵免维护，之后 `1/2/4/8` 分段（@1315400 四段原文） | 第 1 兵即计费 | `math.js@322`、`config.js upkeep` | 中 | S3 | 中 | L1+L2 |
| S5 | **人口结构对齐**：**✅ 已完成（切片 7，2026-09-22）** —— **派生式人口**（由已持久化 `S.tick` 推导，**不新增存档字段**）：基础 4 + `2+5×城镇`/10s，封顶=城镇上限；仅限制新增分配、**不回收既有分配**；C01-C03 + equiv 全绿；curve-sim 仅影响开局 10s，长线不变。**观察 O10/O11**：城镇门使人口无法长期爬升（待 R2/O8 裁决） | 竞品 `2+5×城镇/10s`（@1310081+0x2710）、粮尽 5s 死 3 人（@1840800） | 无增长速率、无衰减 | `math.js@59-61`、`config.js pop`、`ui.js@28/78` | **高** | S3 | 大 | L1+L2 |
| **S9** | **分段斜率升级成本（IE-006 A1）**：⏳ **沙箱研究已完成（2026-09-22）** —— 竞品公式原文 @1485372（前 9 级 ×1 → 之后 ×5/×10/×20/×40）；50h 实测 `seg` 剖面优于我方指数（采集建筑 lv9/7/9 vs 3/3/2、避免后期食物枯竭、升级分布铺开 117 vs 98）。**待你批准后实施**（开关 + 逐值断言 + 50h 前后对比） | 竞品原文 @1485372；`analysis-economy §4`；`reports/s9-cost-profile-study.md` | 我方为指数 `upBase×upCostLv^lv`（形态不同） | `math.js upCost@866`、`config.js` | 中 | S3 | 中 | L1+L2 |
| S6 | **曲线定参复跑**：~~待做~~ **✅ 已完成（切片 8，2026-09-22）**：50h 实测 — 0-3h 过快（0.7h 研完 6 节点）、3-15h 无墙、15-50h 部分对齐（木 35h 撞 210k 上限、食物 18.25h 转负、50h 见底）；产出 G1-G6 根因与目标值候选（见 slice-8 报告） | 竞品骨架（`analysis-difficulty` 五引擎） | 切片3/4/4b/8a/8b 后已大幅改善 | `tests/ie001/curve-sim.js` | 低 | S1-S5 | 中 | L1 |
| **S8a** | ~~**O8 上调单位上限**~~ **✅ 已完成（切片 8a）**：`CFG.unitCapBoost`（步兵 30+10/lv、弓兵 30+6、骑兵 15+4、法师 5+2）；D01-D03 + equiv 全绿 | 竞品兵营线随 LvMax 扩张 | 步兵营 lv5 → 仅 35 兵 | `config.js unitCapBoost`、`math.js@152` | 中 | 无 | 小 | L1+L2 |
| **S8b** | ~~**R2=A 上限改自身 LvMax**~~ **✅ 已完成（切片 8b）**：`CFG.ownMax`（仓库1000／训练50／学院50／生产50／采集50／功能50／营帐10），城镇门拒绝从 top5 消失；E01-E03 + equiv 全绿 | 竞品 `LvMax`（仓库1000、工坊类50）| 全部上限=城镇等级×k（Boss 门） | `config.js ownMax`、`math.js@96` | 中 | 无 | 中 | L1+L2 |
| **S8c** | ~~**R2=A 城镇升级门改「资源+科技」**~~ **✅ 已完成（用户裁决 D2「加入地契」，2026-09-22）**：新增 `CFG.res.deed`（上限 999999）+ `CFG.townGate.useDeed`（地契 5…520 + 科技 0…12000 九档，候选值）；`townCanUpgrade/townGateCost/townGateShortfall/upgradeTown` 改走地契+科技（只扣一次）；旧档补 `deed:0` 不裁剪；关开关时存档不含 deed 键；`deed` 像素图标。**L01-L06 + equiv 全绿**；curve-sim：村民 10→**40**、升级 49→**100**、食物 +3.79→**+37.65/s**；报告 `reports/s8c-deed-town-gate.md` | 竞品城镇需 地契 5/15/30 + 科技 450010/450012 | 城镇升级=击败第 N 关 Boss | `config.js townGate/res.deed`、`math.js townCanUpgrade/upgradeTown`、`ui.js@83`、`sprites.js deed` | 中 | S8b | 中 | L1+L2 |

**S 组总原则**：只调**数值与结构形态**，不改命名/题材/兵种/关卡语义（皮）；每项独立开关；每项完成后用 curve-sim 复跑对照三阶段预算。

## A 基础设施（先做，低风险，零行为变化）
| 编号 | 改造项 | 影响模块 | 来源 | 风险 | 依赖 | 量级 | 验证层 |
|---|---|---|---|---|---|---|---|
| A1 | ~~`tests/ie001/curve-sim.js` 推演脚本~~ **✅ 已完成（切片 1，2026-09-22）** —— 另产出 4 项结构性发现 F1-F4 与 4 条计划修正建议 R1-R4（见 `reports/slice-1-curve-sim.md` 附录） | 新增测试文件 | IE-006 §5 S1；U-S8a 裁决 | 低 | 无 | 中 | L1 |
| A2 | ~~诊断环形日志（内存 50 条、不入存档、零上报）+ 关键失败点埋点~~ **✅ 已完成（切片 2，2026-09-22）**：`logDiag/diagSnapshot/diagClear` + `_warnUnsaved` 埋点；W02/W03/W04 断言 | `math.js`（新增 `logDiag`） | S7；U-S7a | 低 | 无 | 小 | L1+L2 |
| A3 | ~~双层开关骨架（默认关闭）~~ **✅ 已完成（切片 2，2026-09-22）**：`CFG.offline/idem/caps/upkeep/tech/passive/diag` + `market.multiRate/dailyLimit`；W01 断言 + 零行为对拍 | `config.js` + 读取点 | S6b；U-S6b | 低 | 无 | 小 | L1+L2 |

## B 核心能力（依赖 A）
| 编号 | 改造项 | 影响模块 | 来源 | 风险 | 依赖 | 量级 | 验证层 |
|---|---|---|---|---|---|---|---|
| B1 | ~~**存档 v3 骨架**~~ **✅ 已完成（切片 9，2026-09-22）**：`CFG.save.v3` 门控；`ops/offline.pendingReport/daily` 三字段；v2/legacy→v3 迁移幂等、坏 v3 字段进保护、v4 拒收、v3 往返保留；F01-F06 + 11 处口径更新 + equiv（关闭＝与 HEAD 逐字节相同）全绿 | `math.js@431-500`、`config.js save` | S3.1；U-S3a | **中高**（存档兼容） | A3 | 中 | L1+L2 |
| B2 | ~~**幂等层**~~ **✅ 已完成（切片 10，2026-09-22）**：`CFG.idem`（窗口 5s、环形 ≤200、持久化入 v3 `ops`）；打点于 资源科技/兵种研究/导入/恢复；G01-G05 全绿（含"重复导入不二次覆盖、PRE 不重写"）；对拍关闭＝与 HEAD 逐字节相同 | `math.js`（幂等原语+researchScience/commitSaveData/restoreBackupByText）、`technology.js upgradeUnit` | S4；U-S4a/b | 中 | B1 | 中 | L1+L2 |
| B3 | ~~**离线结算**~~ **✅ 已完成（切片 11，2026-09-22）**：`CFG.offline.enabled=true`（0.6×/24h/120s）；`offlineNetRates/offlineDeltaSec/settleOffline/dismissOfflineReport`；启动+回前台各一次；断粮按可支付秒数截断；报告卡 + 落库；H01-H07 + **L2 端到端 6 条**（含截图 `slice11-offline-report-360.png`）全绿。**修复真 bug**：木/石被误判为被动资源致净速率为 0（探针 `tools/verify/probe-offline.js` 复现） | `math.js`（结算模块）、`ui.js`（启动/回前台/报告卡）、`config.js offline` | S2；C1 裁决 | 中 | A2 A3 B1 | 大 | L1+L2（**L3 未运行**） |
| B4 | ~~**离线推进**~~ **✅ 已完成（切片 12，2026-09-22）**：等价抽出 `advanceBuildingsBy(secs)`；`offlineAdvanceSec` 闭式推进建筑/城镇（走同一完成路径）+ 队列复用真实 `processQueue`（时间预算+事件上限 120）；**驻军冻结**（不调 garrisonTick）；J01-J06 + equiv 全绿。**修复 off-by-one**（完成通道多扣 1 秒，J06 以闭式 vs 逐秒对拍锁死） | `math.js`（advanceBuildingsBy/offlineAdvanceSec/settleOffline 衔接）、`garrison.js`（仅冻结，未改） | S5；U-S2b/S5a/S5b 裁决 | 中高 | B1 B3 | 大 | L1（闭式 vs 逐 tick 对拍）+L2 |

## C 对齐回改（高风险，须在 B 稳定后；改动 IE-007/IE-008 已上线模型）
| 编号 | 改造项 | 影响模块 | 来源 | 风险 | 依赖 | 量级 | 验证层 |
|---|---|---|---|---|---|---|---|
| C1 | **科技收入改占人口学者制**（竞品：学者 1 知识/s/人，`Need=[人口,1]`）；学院保留为建筑/加成，收入侧改为占人口；分配 UI 复用 `popAlloc` 或新增轨道 | `config.js`（res.tech/basePerPop 或新轨）、`math.js`（prodRate/tick）、`ui.js`（分配 UI）、`technology.js`（门槛联动） | **C2 裁决**；S8 | **高**（经济核心+人口耦合） | A1（目标值）B1 | 大 | L1+L2 |
| C2 | **被动建筑人口约束**：矿井/冶炼厂/铸币厂/学院 按"占人口"模型改造（细则待定：占 N 人 / 无人口停产） | `config.js@280-288`、`math.js@529`（passiveProduction） | **C5 裁决** | **高** | C1 | 大 | L1+L2 |
| C3 | ~~**交易所多汇率+日限+损失**~~ **✅ 已完成（切片 13，2026-09-22）**：汇率 4→10 条（跨资源对构成价差损失）、`localDay` 本地 0 点日界、日限 5 次 + 跨界重置、失败不改资源；K01-K05 + equiv 全绿；面板显示剩余次数 | `config.js market`、`math.js`（localDay/dailyResetIfNeeded/exchangeResource）、`ui.js@383` | **C3 裁决**；S6.1 | 中 | B1（`daily` 字段） | 中 | L1（汇率/套利不变式/日限/跨日重置）+L2 |
| C4 | **停产可见标识**：原料不足时建筑卡显示"原料不足已停产"（对齐竞品"工人罢工清零"语义） | `ui.js`（`rBuildCard`） | S5 附属；V-C2 | 低 | 无（可并行） | 小 | L2 |

## D 文档与流程
| 编号 | 改造项 | 影响模块 | 来源 | 风险 | 依赖 | 量级 | 验证层 |
|---|---|---|---|---|---|---|---|
| D1 | ~~版本号纪律与发布检查清单~~ **✅ 已完成（切片 16）**：`发布检查清单.md`（6 步 + 3 反例 + 回滚关系）；`index.html` 增 `APP_VERSION/APP_SCRIPTS` 自检锚点；P01（L1）+ L2 断言 | `index.html@1069-1081`、流程文档 | S6.2 | 低 | 无 | 小 | L4（本地自检替代） |
| D2 | ~~数值配置化边界表~~ **✅ 已完成（切片 16）**：`数值配置化边界表.md` + 只读审计脚本；结论"本轮未新增硬编码阈值"，越界候选 V1-V5 登记不改 | 新增文档 + `tools/verify/config-boundary-audit.js` | G6 | 低 | 无 | 小 | 文档审查 |
| D3 | ~~每片落地结果回写~~ **✅ 已完成（切片 16）**：`策划基线-v1.md` §11「实施进度」（16 切片 × 开关默认值 + 未实施项） | `docs/codex/plan/*` | 全局纪律 | 低 | 每片 | 小 | 文档审查 |

## 依赖图（执行顺序约束）
```
A1 ────────────────┐
A2 ─┐              │
A3 ─┴─→ B1 ─→ B2   │
        └─→ B3 ─→ B4│
                └───┴─→ C1 ─→ C2
        B1 ─→ C3            └─→ C4(可并行)
D1 D2 D3：随时可做（D3 随每片）
```

## 风险总览
| 等级 | 项 | 主要风险 | 缓解 |
|---|---|---|---|
| **高** | C1 C2 | 改变经济/人口核心模型，且**回改已上线实现**；影响面含存/读档与平衡 | 独立切片、开关控制、先 A1 推演定参、每片实机验证、失败即回退 |
| **中高** | B1 B4 | 存档版本升级（兼容风险）；tick/驻军状态机被触碰 | v3 迁移测试矩阵、闭式对拍（vs 逐 tick 小样本）、驻军冻结最小改动 |
| 中 | B2 B3 C3 | 幂等持久化、离线结算边界、汇率/日限 | 窗口边界用例、断粮/前跳/回拨用例、套利不变式断言 |
| 低 | A1-A3 C4 D1-D3 | 新增文件/默认关闭开关/纯 UI 标识 | 基线回归全绿即放行 |

## 不做的（明确排除，避免顺手扩大）
- 不引入服务端/依赖/中间件（硬约束）；不做按比例灰度（无服务端，用开关替代）
- 不新增周目字段（C6 裁决）；不改伤害公式/Boss 编成/关卡数值（IE-001 禁改范围）
- 不重构 `math.js` 结构（只做定点新增/替换）
