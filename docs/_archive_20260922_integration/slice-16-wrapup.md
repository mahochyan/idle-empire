# 切片 16 报告 · 收尾（D1 版本号纪律 / D2 配置化边界表 / D3 文档同步）

## 1. 范围（3 项，均为文档/自检，不改玩法数值）
| 项 | 产出 | 证据 |
|---|---|---|
| **D1 版本号纪律** | `发布检查清单.md`（机制事实 + 每次上线 6 步 + 3 条反例 + 与回滚的关系）；`index.html` 增加可自检锚点 `window.APP_VERSION` / `window.APP_SCRIPTS` | 自检用例 `P01`（L1）+ smoke `切片16 L2` 断言 |
| **D2 数值配置化边界表** | `数值配置化边界表.md`（边界规则 + 审计分布 + 合规清单 + 6 项越界候选 V1-V6） | 只读审计脚本 `tools/verify/config-boundary-audit.js`，日志 `logs/config-boundary-audit.log` |
| **D3 文档同步** | `策划基线-v1.md` 新增 §11「实施进度」（16 个切片 × 开关与默认值 + 未实施项 + 发布/边界表引用） | 基线 §11 |

## 2. L1（命令/输出/退出码）
```
node --check tests/ie001/run.js → exit 0
node tests/ie001/run.js         → 通过 90 / 失败 0，exit 0
  [PASS] P01 index.html 版本锚点/加载顺序/协议感知齐全
         （断言：var V=<数字>、APP_VERSION/APP_SCRIPTS、http(s) 加 ?v=、7 个脚本顺序严格）
node tools/verify/equiv.js      → exit 0（全开关关闭 ⇒ 与 HEAD 逐字节相同）
```

## 3. L2（真实浏览器）
```
node tests/ie001/browser_smoke.js    → 41/41 exit 0
  [PASS] 切片16 L2：版本锚点与脚本加载顺序可自检（APP_VERSION='1'、APP_SCRIPTS 顺序正确）
node tests/ie001/browser_interact.js → 28/28 exit 0
清理核对：两套件 headless 残留 0
```

## 4. D2 审计要点（真实数据）
| 文件 | 数值字面量种类 | 结论 |
|---|---|---|
| `math.js` | 85 | 多为公式常量（0/1/2/0.5）与从 CFG 读取的参数；**6 项越界候选**（哨兵 `999`、告警节流 `30000`、保存周期 `%60` 等） |
| `technology.js` | 28 | **本身即配置层**（兵种研究树 500/600/1500/2000…），合规 |
| `garrison.js` | 48 | 入侵模板奖励/时长硬编码在逻辑层 → **V5 越界候选**（迁到 `CFG.invasions` 属重构，另立切片） |
| `ui.js` | 23 | 格式化/布局常量，合规 |

**关键结论**：本轮改造（切片 2-16）**未新增任何硬编码阈值**——所有新参数都落在 `config.js` 开关表；越界项 V1-V5 均为**既有历史**问题，仅登记不改。

## 5. 阈值达成
| 阈值 | 结果 |
|---|---|
| 版本锚点可自检（含顺序与协议感知） | ✅ P01（L1）+ L2 |
| 发布纪律成文且含反例 | ✅ `发布检查清单.md` |
| 数值边界成文且有审计数据 | ✅ `数值配置化边界表.md` + 审计日志 |
| 文档与代码同步（滑块/开关/默认值） | ✅ 基线 §11 |
| 未新增硬编码阈值 | ✅ 审计结论 |
| 回归全绿 | ✅ L1 90/90、L2 41/41 + 28/28 |

## 6. 结论
**通过**。至此**批准计划内的可实施切片（1-13、16）全部完成**；剩余切片 14/15 与数值对齐（S2/S9/S8c）**等待你的裁决**，原因与影响已在基线 §11 与各切片报告中列明。
