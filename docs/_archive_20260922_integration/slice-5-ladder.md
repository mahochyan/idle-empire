# 切片 5 报告 · S2 成本阶梯扩节点（骨架拟合）

## 1. 切片名与范围
- 切片：**S2 成本阶梯扩节点**（`分期计划.md` 切片 5）
- 目标：把资源科技从"3 节点跨 2 个数量级"扩为"多步递进"的阶梯形态（对齐竞品 `developScienceList` 40+ 节点、每步一门槛的**节奏形态**）
- 改动（4 个生产文件 + 测试/工具）：
  - `config.js`：新增 `CFG.sciencesLong`（6 节点：探矿术 100 → 冶铜术 300 → 冶金术 800 → 冶铁术 1800 → 铸币术 5000 → 货币铸造 12000）；`CFG.tech.longLadder: true`
  - `math.js`：新增 `activeSciences()/sciName()/sciIdKnown()`；`researchScience` 用生效表；`validateSave` 用**并集**校验（旧档 3 节点 id 仍合法）；`buildAct` 提示名走 `sciName`
  - `technology.js`：科技页遍历 `activeSciences()`；**展示同源**（战功显示用生效值 0，前置名用 `sciName`）
  - `ui.js`：建筑卡"需先研究"提示名走 `sciName`
  - 测试/工具：新增 A01-A03；X03 改为遍历生效表；V08/X01/X02 明确"原始 3 节点口径"；`equiv.js` 关闭清单加 `longLadder`；`curve-sim`/`reachability` 改为读生效表
- 成本数值为 **【候选·待推演】**（本轮由 curve-sim 验证节奏）；原始 `CFG.sciences`（3 节点，10/80/200）**未改**
- 回滚：`CFG.tech.longLadder=false`（A01/A03 证明回到原始表且可逆）

## 2. L1 验证
```
node --check config.js/math.js/technology.js/ui.js/tests/*.js/tools  → exit 0
node tests/ie001/run.js                                             → 通过 48 / 失败 0，exit 0
  [PASS] A01 开关关闭→原始 3 节点表（10/80/200，链式前置仍强制）
  [PASS] A02 开关开启→6 节点长阶梯；未研前置时拒绝；按序 6/6 成功；解锁节点 id 保持稳定（兼容旧档）
  [PASS] A03 开→关可逆；旧档 [sci_copper,sci_iron,sci_coin] 在长阶梯下仍校验通过；节点名正确
  [PASS] X03 可达性：前置图完整无环 + 每个解锁建筑可建（长阶梯 6/6）
node tools/verify/equiv.js                                          → exit 0（全开关关闭 ⇒ 存档剔 ts 与 HEAD 逐字节相同）
node tools/verify/reachability.js                                   → exit 0：资源科技 6/6 纯挂机可达
```

## 3. L2 验证
```
node tests/ie001/browser_smoke.js    → 通过 34 / 失败 0，exit 0
node tests/ie001/browser_interact.js → 通过 28 / 失败 0，exit 0
```
截图：科技页节点数变化（3→6）属内容变化，未单独截图；如需可补（列入后续 UI 证据）

## 4. L3 / L4 状态
L3 未运行（无设备）；L4 未部署（M2 骨架完成后统一部署）

## 5. curve-sim 前后对比（10h，默认均衡）
| 指标 | 切片4b 后（3 节点） | 切片5 后（6 节点） |
|---|---|---|
| 研究节奏 | 冶铜@10s → 冶铁@30s → 货币@70s | **探矿@30s → 冶铜@90s → 冶金@250s → 冶铁@600s → 铸币@1100s → 货币@2520s** |
| 研究完成数 | 3/3 | **6/6** |
| 升级启动 | 42 | **42（未被拖累）** |
| 建筑终态 | 仓库3/矿井6/冶炼4/铸币6/市场5/学院20 | **同前**（仓库3/矿井6/冶炼4/铸币6/市场5/学院20） |
| 科技点终值 | 43000 | 43000 |
| 拒因（新增） | — | `需先研究「货币铸造」×506`、`科技点不足×252`（阶梯产生的真实门槛） |

**判读**：阶梯把"一次性解锁"改成"多步递进"，且**未拖累经济**（升级启动仍 42）——即竞品"每步一个知识门槛"的节奏形态已在我方尺度上成立。

## 6. 阈值达成情况
| 阈值 | 结果 |
|---|---|
| 节点数/跨度形态对齐（本轮范围） | ✅ 3→6 节点；跨度 100→12000（≈2.1 个数量级；竞品 11 个数量级需"时代内容"支撑，见 §附） |
| 链式前置被强制 | ✅ A02（未研前置即拒） |
| 全链可达（纯挂机） | ✅ reachability 6/6 + curve-sim 6/6 |
| 旧档兼容（sciences id 并集校验） | ✅ A03 |
| 展示与实现同源 | ✅ 科技页战功显示生效值、前置名走 `sciName` |
| 关闭开关 = 原始表 | ✅ A01 |
| 全开关关闭 ⇒ 与 HEAD 逐字节等价 | ✅ equiv |
| 回归全绿 | ✅ L1 48/48、L2 34/34、28/28 |

## 7. 断言更新披露
| 用例 | 变更 | 原因 |
|---|---|---|
| `V08`/`X01`/`X02` | 显式 `CFG.tech.longLadder=false`（并在标题标注"原始 3 节点口径"） | 三例本意是验证**原始链**机制（10/80/200 与战功路径）；长阶梯由 A02/A03 覆盖。**旧行为覆盖未被删除** |
| `X03` | 前置图遍历改为"生效表"（长阶梯 6 节点） | 使可达性检查跟随实际生效配置（更强） |

## 8. 回退验证
- 回滚：`CFG.tech.longLadder=false` → 立即回到原始 3 节点表（A01/A03 静态证据）
- 彻底回退：`git checkout -- config.js math.js technology.js ui.js`
- 未实际执行回退（本片无失败）

## 9. 结论
**通过**（L1 ✅ / L2 ✅ / 阶梯形态达标（本轮范围）✅ / 可达 ✅ / 旧档兼容 ✅ / 零行为对拍 ✅；L3 未运行、L4 未部署）

## 附：S2 的剩余部分（**不在本片范围，需内容支撑**）
| 项 | 现状 | 说明 |
|---|---|---|
| 跨度 11 个数量级（竞品 200→1e13） | 我方 100→12000 | 竞品跨度靠"时代节点"（蒸汽 1e5、电力 5e6、核能 1e8、量子 3e9、神话 1e13）；我方需先有对应**内容/时代系统**（皮），否则会变成"解锁空节点" |
| 兵种研究阶梯（竞品 60 节点） | 我方 4 线 24 节点 | 需要兵种线内容扩展（属后续内容切片） |
