# 放置帝国 · Codex 工作约定

适用仓库：`mahochyan/idle-empire`。
目标：先保证存档安全、核心成长可达、战斗结算可信，再增加离线与长期内容。
本文是项目级工作规则，不是一次性实施全部规划的授权。当前任务以用户本次指定的任务单为准。

## 1. 开工与事实基线

- 先读取工作区已有的适用 AGENTS 指令、`CLAUDE.md`、`index.html`，确认真实文件结构、脚本顺序和测试入口。不得覆盖既有 AGENTS 规则，应保留并合并。
- 运行并记录 `git status --short`、`git branch --show-current`、`git rev-parse HEAD`。保留用户未提交的改动；不得自行 reset、强制 checkout、批量格式化或清空工作区。
- 审查参考快照为 `98571e38801f71bfdbb982a626e0e58f510abb98`，核对日期 2026-09-18。它不是强制回退目标；实际实施必须以当前工作区为准。
- `00_README.md`、`03_长期运营优化方案.md`、`04_实施计划.md`、`05_验证灰度与回滚.md` 是背景方案，不是已实现功能的证据。工作区没有这些文件时如实记录，当前明确任务仍可继续。
- 未获得的 `01_现状审计.md`、`02_对标拆解_放置时代可迁移机制表.md` 不得自行补写或声称已读。
- 文档与代码不一致时，分别记录“文档目标”“代码现状”“本次决定”。不得把待校准数值或此前聊天中的推测直接当作已验证事实。

## 2. 范围与架构

- 保持原生 HTML/CSS/JavaScript、静态运行和现有像素风移动端布局。没有明确任务，不迁移框架、不引入服务器、账号系统、付费或运行时第三方依赖。
- 配置属于 CFG，持久化游戏状态属于 S，战斗临时状态属于 B。先检查函数调用关系，再决定新增代码位置。
- 当前审查快照的脚本顺序是 `config.js → levels.js → sprites.js → math.js → garrison.js → technology.js → ui.js`。实施前从 index.html 重新确认；不得无理由调整依赖顺序。
- 不照旧方案的文件名盲目创建 `buildings.js`。先找真实的建筑配置；需要新文件时说明最小必要理由。
- 一张任务单对应一个可审查改动集。无关问题写入后续清单，不顺手重构整个 math.js，也不一次性实施六批运营方案。
- 不擅自推送默认分支、合并 PR、发布 Pages 或改部署设置。当前任务没有要求远程写入时，交付本地 diff、测试和报告即可。

## 3. 存档底线

- 主存档键 `rts_save` 的兼容读取必须保留。新增字段要同时覆盖序列化、反序列化、迁移、默认值和测试。
- 解析、校验和迁移先在独立候选数据上执行，成功前不能污染运行中的 S。合法 0 值不得被 `||` 缺省逻辑替换。
- 迁移、导入和恢复前保留可恢复的原始数据。备份失败不能继续覆盖原档。普通自动备份不得抹掉迁移前的独立保护副本。
- 损坏档或不支持的未来版本必须有明确提示并阻止自动写回，不能静默变新档，再被 tick 或其他 save 调用覆盖。
- 历史数据超出新配置上限时，不得默默裁剪兵力、资源或进度；记录冲突并采用不毁档的路径。
- 存储读取、备份或写入失败时，不得显示成功；必须保留现有可恢复数据，并提供手动导出路径。
- 关闭功能不等于删除它的数据。保留存量、已研究节点和已领取记录；兼容性承诺要有测试。
- 回退旧代码与关闭新功能是两种操作。不得宣称“旧版忽略新字段，所以任何回滚都不伤档”。

## 4. 玩法与数值底线

- 内部时间单位明确到秒或毫秒，展示单位单独换算；推演表必须标注单位和假设。
- 科技成长要验收完整链路：获得科技点 → 满足关卡/建筑/前置研究条件 → 扣费一次 → 解锁 → 能训练；不是仅让资源数字增长。
- 当前战斗公式使用 HP 参与伤害计算。修改 Boss HP、护盾或回血前，核查伤害与兵力回写的联动；不能把护盾存成新增士兵。
- 战斗与驻军共享的函数、常量或资源变更，需要同时核对两条路径。
- 离线系统另立任务：统一时间推进，明确队列扣费、建筑完成、仓库、耗粮、系数和驻军规则；不能用初始净速率乘时长冒充动态模拟。
- 奖励、离线领取与导入要验证重复调用和刷新边界；纯本地存档不承诺防所有改时钟或手工篡改行为。
- 确定性故障修复不保留故障对照组。没有真实玩家数据时，不声称改善留存或完成运营 KPI。

## 5. 测试与证据

- 缺陷修复优先补能复现缺陷的测试。调用真实实现，不复制一套游戏公式让测试自我证明。
- 复用现有测试工具。没有工具时采用最小开发侧方案，记录运行环境；测试依赖不得成为玩家运行游戏的前置条件。
- 模拟 localStorage、DOM 或计时器的测试不等于真实浏览器验证。涉及 UI、加载顺序、异步回调时补浏览器冒烟；环境不具备就标注未验证。
- 测试结果分为通过、失败、未运行。不能把计划、代码审查、跳过项或伪造截图写成通过。
- 既有失败要提供基线复现，与新增回归分开。不能删断言、掩盖失败或修改期望只为得到绿色结果。
- 每次交付包含基线/最终 SHA 或 diff、修改清单、实际命令与退出码、验收逐项结果、存档兼容说明、已知风险和回退路径。

## Code Review Rules

重点检查以下问题：

- 错误加载后仍能自动保存；导入失败先改了 S；备份失败后继续覆盖主档。
- 新字段未保存/未迁移；合法 0 被替换；旧档字段被静默裁剪。
- 重复领奖、重复扣费、负数/非有限资源、护盾转化为永久兵力。
- 只改 UI 限制而没有动作函数校验；测试绕开真实调用路径。
- 异步战斗回调向导入后的状态写入；远征与驻军规则意外分叉。
- 无授权的数值调整、框架重构、额外玩法、默认分支推送或部署。

完成当前任务后提交结果，不自动启动下一张任务单。
